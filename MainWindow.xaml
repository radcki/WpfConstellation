<Window x:Class="WpfConstellation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfConstellation"
        mc:Ignorable="d"
        Background="Black"
        Title="Constellation" Height="450" Width="800">
    <Canvas x:Name="AnimationArea" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch"
            Height="{Binding Height, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}" 
            Width="{Binding Width, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
            >
    </Canvas>
</Window>
